```
 // Create Azure Public Load Balancer  
  
 1. Create RG - LB  
 2. Create VNET - LBVNET 
 3. Create 2VMs - VM1 & VM2 (Ubuntu Server) | Standard_D2s_v3 | NSG: http-80, https-443, ssh-22 
  
 cd Downloads  
 chmod 400 key.pem 
 ssh -i key.pem atul@20.51.112.68 
 ssh -i key.pem atul@48.221.120.162 
  
 sudo apt update  
 sudo apt install apache2 -y 
 sudo systemctl start apache2 
 sudo systemctl enable apache2 
 cd /var/www/html  
 sudo rm index.html 
 sudo touch index.html  
 sudo nano index.html 
  
 save <h1>Webserver 1 <h1> on VM1 
 save <h1>Webserver 2 <h1> on VM2 
  
 4. Check public IPs of VMs 
  
 5. Search Load Balancers >> Standard Load Balancer >> Public  
  
 6. Create frontip >> create ip allocation 
  
 7. Crate Bakend pool >> mybeckendpool >> include VMs via selecting VNet   
  
 8. Create LBRule >> 80, 80 
  
 9. Create HealthProbe - myhealthprobe >> 80 
  
 10. check load balancer ip in browser  
  
 // Private Azure Load Balancer  
  
 1. Create VM3 | Windows Server 2025 >> RDP >> Open Browser  
 2. Search Load Balancers >> Standard Load Balancer >> Internal 
 3. Create frontip >> create ip allocation 
 4. Crate Bakend pool >> mybeckendpool >> include VMs via selecting VNet   
 5. Create LBRule >> 80, 80 
 6. Create HealthProbe - myhealthprobe >> 80 
 7. check load balancer ip in browser
```
